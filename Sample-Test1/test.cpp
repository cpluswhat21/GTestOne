#include "pch.h"

TEST(TestCaseName, FirstTest) {
  EXPECT_EQ(1, 1);
  EXPECT_TRUE(true);
}

TEST(TestCaseName, SecondTest) {
	EXPECT_EQ(1, 1);
	EXPECT_TRUE(true);
}

TEST(TestCaseName, ThirdTest) {
	EXPECT_EQ(1, 1);
	EXPECT_TRUE(true);
}